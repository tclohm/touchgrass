<!DOCTYPE html>
<html>
  <head>
    <title>Touchgrass Rental</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <nav class="bg-blue-600 shadow-lg">
      <div class="max-w-7xl mx-auto px-4">
        <div class="flex justify-between h-16">
          <div class="flex items-center">
            <%= link_to "🏰 Disneyland Pass Rental", root_path, class: "text-white text-xl font-bold" %>
          </div>
          
          <div class="flex items-center space-x-4">
            <% if user_signed_in? %>
              <%= link_to "My Passes", pass_rentals_path, class: "text-white hover:text-blue-200" %>
              <%= link_to "My Bookings", bookings_path, class: "text-white hover:text-blue-200" %>
              <%= link_to "List Pass", new_pass_rental_path, class: "bg-white text-blue-600 px-4 py-2 rounded-md hover:bg-gray-100" %>
              <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "text-white hover:text-blue-200" %>
            <% else %>
              <%= link_to "Login", new_user_session_path, class: "text-white hover:text-blue-200" %>
              <%= link_to "Sign Up", new_user_registration_path, class: "bg-white text-blue-600 px-4 py-2 rounded-md hover:bg-gray-100" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>
    
    <main class="max-w-7xl mx-auto py-6 px-4">
      <% flash.each do |type, message| %>
        <div class="mb-4 p-4 rounded-md <%= type == 'notice' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %>">
          <%= message %>
        </div>
      <% end %>
      
      <%= yield %>
    </main>
  </body>
</html>
